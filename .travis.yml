language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "sorbreaker"
  - git config user.email "sorbreaker@sina.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${travis-ci}@${GH_REF}" master:gh-pages
# E: Build LifeCycle

branches:
  only:
    - hexo1
env:
 global:
   - GH_REF: github.com/sorbreaker/sorbreaker.github.io.git
